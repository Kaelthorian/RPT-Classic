# BYOND compiler/runtime outputs created locally. Existing tracked legacy artifacts
# remain tracked until the project deliberately migrates them out of version control.
*.dmb
*.dyn.rsc
errors.log

# Local editor and OS files.
.vscode/
.idea/
Thumbs.db
.DS_Store
